{
  "name": "@rostrum/common",
  "version": "1.0.0",
  "main": "dist/index.js",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^17.0.21"
  },
  "scripts": {
    "lint": "ts-standard src",
    "compile": "tsc",
    "clean": "rm -rf dist",
    "build": "yarn lint && yarn clean && yarn compile"
  },
  "dependencies": {
    "axios": "^0.26.0"
  }
}
